# 【开发】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发环境设置为 / 使用代理，生产环境设置为完整后端地址）
VITE_API_URL = /

# 本地开发统一代理到 local Profile 后端；txcloud 验证通过部署后的同源入口完成。
VITE_API_PROXY_URL = http://127.0.0.1:8080

# Delete console
VITE_DROP_CONSOLE = false
